argo-cd:
  server:
    metrics: &monitoring-metrics
      enabled: false
      serviceMonitor:
        enabled: false
  controller:
    metrics: *monitoring-metrics
  repoServer:
    metrics: *monitoring-metrics
  redis:
    metrics: *monitoring-metrics
