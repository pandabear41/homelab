apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: plex
  name: kube-plex-data-nfs-pvc
spec:
  volumeName: kube-plex-data-nfs-pv
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 500Gi